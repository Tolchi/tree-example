%h2 Contact us

%p
  해주실 말씀이 있으면 아래 form을 이용해 메일을 보내주세요.
%p 

= simple_form_for @message, :url => contact_path, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name, :label => "이름"
  = f.input :email, :label => "이메일", :placeholder => "user@domain.com"
  = f.input :subject, :label => "제목"
  = f.input :body, :label => "내용", :as => :text
  .form-actions
    = f.button :submit, '보내기', :class => 'btn btn-primary'
