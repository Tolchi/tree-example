.span2
  %h3.lead 카테고리 
  - is_admin = (user_signed_in? and current_user.has_role? :admin)
  - if is_admin
    = link_to t('.new', :default => t("helpers.links.new")), new_category_path, :class => 'btn btn-primary'
  = sortable_tree @categories, :path => 'categories/tree/base', :title => :name, :js => true
