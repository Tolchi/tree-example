.span10
  - cat = @category || Category.new
  - if cat.name.blank?
    - categories = Category.roots
    %section#categories
      .row
        .span10.category_title.lead 톱 카테고리
      .row
        .span9.offset1
          .row
            - categories.each do |c|
              .span3
                .gray14lightbullet
                  = link_to c.name, c
                  %span.descendants
                    = c.descendants.count +  Company.find_all_by_category_id(c.id).count
  - else
    %section#categories
      .row
        .span10.category_title.lead
          카테고리:
          = @category.name
      - if @children
        .row
          .span10.category_title.lead
            하위 카테고리
        .row
          .span9.offset1
            .row
              - @children.each do |c|
                .span3
                  .gray14lightbullet
                    = link_to c.name, c
                    %span.descendants
                      = c.descendants_count + Company.find_all_by_category_id(c.id).count
      - if @category.child?
        .row
          .span10.category_title.lead
            상위 카테고리
        .row
          .span9.offset1
            .row 
              .span3
                - parent = @category.parent
                .gray14lightbullet
                  = link_to parent.name, parent
    %p
    - unless @coms.blank?
      %section#companies
        .row
          .span10
            .row
              .span10.category_title.lead
                회사
            .row.companies-list
              .span9.offset1
                .row
                  - @coms.each do |com|
                    .span3.paginated
                      .gray14lightbullet
                        = link_to com.name, com_path(:id => com.id)
                  = will_paginate @coms, :renderer => BootstrapPagination::Rails, :class => "pagination pagination-centered"
            - unless @json == "[]"
              .row
                .span9.offset1
                  = gmaps("map_options" => { "mapTypeControl" => false, "maxZoom" => 15, "detect_location" => true, "zoom" => 12}, "markers" => {"data" => @json})
