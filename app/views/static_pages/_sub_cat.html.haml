- @root = @category.root
- @categories_same_father = @root.descendants.nested_set.all
%h3= @root.name
= sortable_tree @categories_same_father, :path => 'static_pages/tree/base', :title => :name, :js => true
