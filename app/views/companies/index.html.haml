- provide(:title, "업소록 | ")
- provide(:keyword, "아르헨티나 한인 네트워크 온라인 업소록 데이터 베이스 Argentina coreano online Directory Database lista de empresas.")
- provide(:description, "아르헨티나 한인 네트워크 데이터 베이스에 포함된 업소목록 입니다. Argentina Hanin Coreano online directory. Lista de empresas incluidas a la bases de datos de Argentina Hanin Network. ")
.row
  = render 'layouts/messages'
  = render 'layouts/breadcrumbs'
  .span12
    .row
      .page-header
        %h1 업소록
  -if @companies.blank?
    .span3 
      = link_to "Back", :back, :class => "btn btn-small btn-info"
  -else
    .span12
      .row
        = render 'companies/table'
