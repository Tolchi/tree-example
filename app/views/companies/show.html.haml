- provide(:title, "회사정보 - #{@company.name}")
.row
  = render 'layouts/messages'
  = render 'layouts/breadcrumbs'
  .span12
    .row
      = render 'companies/company_info'
      - if @company.gmaps?
        .span8
          = render ('companies/gmaps')
